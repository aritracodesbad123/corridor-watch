# Audit tampering

SQLite rows after UPDATE/DELETE: 0. mutated_or_deleted=True. Postgres production should use an append-only role. This test documents the current store; it does not claim a hash chain.
